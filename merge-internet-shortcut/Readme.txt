このフォルダの1つ上のディレクトリにあるインターネットショートカットを、1つのhtmlファイルに統合します。

使い方:
	merge_InternetShortcut.exeを実行。
	実行後、フォルダ内に「(現在時刻).html」というhtmlファイルが生成されます。

	htmlファイルをブラウザで開くことで、複数のインターネットショートカットで指定していたリンクがこのhtmlファイルに統合されていることを確認できます。

その他:
	.batファイルは消さないでください。